# NeonVault-Secure-Login-System
(Flask + SQLAlchemy + Flask-Login + Flask-WTF + Bootstrap 5)  Features:  User registration  Login authentication  Password hashing  Flask-Login session handling  Flask-WTF forms  Bootstrap styled UI  Neon / dark theme dashboard  Logout  Protected routes
